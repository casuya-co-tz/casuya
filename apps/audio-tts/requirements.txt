fastapi==0.141.1
uvicorn[standard]==0.52.4
gunicorn>=26.1.0
pydantic==2.13.4
pydantic-settings==2.15.0
numpy>=1.26.4
# sherpa-onnx is installed from the Dockerfile during build, not from here;
# it pulls onnxruntime + numpy at build time. Voice models are baked into the
# image at build time too (see the Dockerfile).
# Casuya Audio-TTS microservice (Sherpa-ONNX TTS, Railway-hosted).
# Same internal key the platform backend sends as X-API-Key on every call.
CASUYA_AUDIO_TTS_API_KEY=

# Where the baked-in voice packs live (matches the Dockerfile ARG).
# SHERPA_MODELS_DIR=/opt/sherpa-onnx-tts/models
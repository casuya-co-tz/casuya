# Casuya Audio-STT microservice (Sherpa-ONNX ASR, Railway-hosted).
# Same internal key the platform backend sends as X-API-Key on every call.
CASUYA_AUDIO_STT_API_KEY=

# Should match the platform MediaRecorder sample rate.
# SHERPA_SAMPLE_RATE=16000
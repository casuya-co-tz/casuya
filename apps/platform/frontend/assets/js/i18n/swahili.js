// i18n/swahili.js — merged Swahili dictionary. Loaded LAST after i18n/swahili/*.js
// on static pages (classic scripts) and concatenated into role bundles. Exposes
// the single global `SW` (the object previously declared inline in this file).

var SW = window.SW;
# docker-compose environment (container networking uses service names)
# Copy to infra/.env when running docker compose -f infra/docker-compose.yml

POSTGRES_DB=casuya_platform
POSTGRES_USER=postgres
POSTGRES_PASSWORD=Mkalanga1994!@

# Backend uses these inside containers (postgres/redis are service names)
DATABASE_URL=${NEON_DATABASE_URL}
REDIS_URL=redis://redis:6379/0

JWT_SECRET=change-me-in-production
# Payment gateway (fill with real values from Azampay dashboard)
AZAMPAY_CLIENT_ID=
AZAMPAY_CLIENT_SECRET=

# Neon serverless Postgres (populated from secret by deploy CI)
NEON_DATABASE_URL=postgresql://neondb_owner:npg_RKL8SyPAl9NW@ep-small-water-b2p0xdhw-pooler.c-6.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require
